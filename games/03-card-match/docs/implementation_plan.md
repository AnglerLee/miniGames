# 구현 계획서 - 03-card-match 개선

카드 맞추기 게임(Card Match)의 기능을 개선하기 위한 계획입니다. 난이도 설정을 관리자 페이지로 이동시키고, 카드 쌍 개수 조절(6~30쌍), 제한 시간 타이머 및 동적 재도전 로직을 구현하며, 게임 UI를 정리할 예정입니다.

## 사용자 검토 필요 사항

> [!IMPORTANT]
> - **난이도 조절 방식 변경**: 게임 내의 난이도 버튼(쉬움/보통/어려움)은 삭제됩니다. 대신 관리자 페이지에서 슬라이더를 통해 카드 쌍의 개수를 직접 설정하게 됩니다.
> - **UI 변경**: 화면 공간 확보를 위해 "시도 횟수"와 "콤보" 표시는 메인 게임 화면에서 제거됩니다. "재시도" 버튼은 상단 타이틀 우측으로 이동합니다.
> - **카운트다운 & 매칭**: 짝이 맞춰진 카드는 화면에서 사라집니다. 제한 시간 종료 시 팝업을 통해 재도전(시간 증가)을 선택할 수 있습니다.
> - **UI/UX 개선 (New)**: 타이머 중앙 정렬, 브라우저 기본 Alert 대신 커스텀 모달 사용, 카드 그리드 시인성 개선.
> - **모바일 최적화 (New+)**: 타이머 크기 축소, 불필요한 설정 정보 텍스트 제거, 여백 최소화로 한 화면에 게임이 들어오도록 조정.
> - **디자인 변경 (New++)**: 주 타겟(여자 아이)에 맞춰 카드 뒷면을 "귀여운(Cute)" 이미지로 변경.

## 변경 제안 사항

### 관리자 설정 (Admin Settings)

#### [admin.html](file:///d:/Angler/miniGames/games/03-card-match/admin.html)
- `pairCount` (카드 쌍 개수) 조절을 위한 슬라이더 입력 추가 (범위: 6 ~ 30, 단위: 1).
- `timeLimit` (제한 시간) 설정을 위한 숫자 입력 추가 (초 단위).

#### [admin.js](file:///d:/Angler/miniGames/games/03-card-match/admin.js)
- `loadSettings`: `localStorage`에서 `pairCount`와 `timeLimit` 값을 불러오는 기능 구현 (키: `card_match_settings`).
- `saveSettings`: 설정된 값을 저장하는 기능 구현.
- 기본값: `pairCount: 8`, `timeLimit: 60`.

### 게임 로직 및 UI (Game Logic & UI)

#### [index.html](file:///d:/Angler/miniGames/games/03-card-match/index.html)
- **헤더**: 재도전 버튼을 타이틀 우측으로 배치. 타이틀 폰트 사이즈 조정.
- **타이머 영역**: `.stats-container`의 높이를 줄이고, 타이머 폰트 사이즈를 축소 (3rem -> 1.5rem~2rem). 상하 마진 축소. "남은 시간" 라벨 제거 고려 또는 작게 표시.
- **게임 영역**: Cards Grid의 마진 축소. 화면 꽉 차게 배치.
- **설정 텍스트**: `#bestRecord` 요소 숨김 또는 제거 (게임 내 설정 표시 삭제).

#### [game.js](file:///d:/Angler/miniGames/games/03-card-match/game.js)
- **커스텀 모달 구현**: `alert`, `confirm` 대신 `showCustomModal` 사용.
- **정보 표시 제거**: `loadBestRecord` 함수에서 텍스트 설정하는 부분 제거 (`bestRecordEl.textContent = ''`).

#### [style.css] (index.html 내부 등)
- `.stat-value.timer`: 폰트 크기 줄임, 그림자 효과 은은하게 변경.
- `.game-area`: padding 축소.
- `.container`: 모바일에서 padding 축소.
- Grid gap 축소 (10px -> 5px).
- `.card-back`: 기존 그라데이션 대신 **생성된 귀여운 패턴 이미지**를 배경으로 사용.
    - `background-image: url('assets/card_back.png');`
    - `background-size: cover;`
    - 이모지(🎴) 제거.

### 이미지 생성 (Image Generation)
- **자산**: `games/03-card-match/assets/card_back.png`
- **프롬프트**: "Cute pastel pattern background, hearts and stars, pink and light blue colors, childish style, seamless wallpaper --no text"

## 검증 계획 (Verification Plan)

### 수동 검증 (Manual Verification)
1.  **UI 레이아웃**:
    - 모바일 뷰포트에서 타이머가 너무 크지 않은지 확인.
    - 설정 텍스트("현재 설정: 8쌍...")가 보이지 않는지 확인.
    - 전체 게임 화면이 한눈에 들어오는지 확인 (스크롤 최소화).
2.  **디자인**:
    - 카드 뒷면이 귀여운 이미지로 변경되었는지 확인.
